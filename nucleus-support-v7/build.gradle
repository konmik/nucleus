apply plugin: 'com.android.library'

android {
    compileSdkVersion 21
    buildToolsVersion "22.0.0"

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 21
        versionCode 3
        versionName "1.1.0"
    }
}

configurations {
    compileJavadoc
}

dependencies {
    compile 'com.android.support:appcompat-v7:22.0.0'
    compile 'info.android15.nucleus:nucleus:1.1.0'
    compileJavadoc 'com.android.support:appcompat-v7:22.0.0'
    compileJavadoc 'info.android15.nucleus:nucleus:1.1.0'
}

apply from: '../gradle/gradle-mvn-push.gradle'
